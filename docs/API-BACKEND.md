# Backend API

The authoritative route table, auth model, and database schema summary live in the root
**`CLAUDE.md`** ("Architecture — backend"). Implementation: `src/app/api/v1/**/route.ts`, helpers
in `src/lib/`. This file exists only so old links resolve.
